export default function(logDate, logMessage, level, parentNode, children) {
  this.logDate = logDate
  this.logMessage = logMessage
  this.level = level
  this.parentNode = parentNode
  this.children = children
}
